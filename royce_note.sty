\usepackage[utf8]{inputenc}
\usepackage{balance}
\usepackage[scaled]{helvet}
\renewcommand\familydefault{\sfdefault} 
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{datetime}
\usepackage{dblfloatfix}
\newdateformat{monthyeardate}{%
  \monthname[\THEMONTH], \THEYEAR}

\usepackage{graphicx} % support the \includegraphics command and options
\graphicspath{ {Figures/} {Logos/} }
\usepackage{epstopdf}
\usepackage{subcaption}

\usepackage{float}
\usepackage{booktabs} % for much better looking tables
\usepackage{array} % for better arrays (eg matrices) in maths
\usepackage{paralist} % very flexible & customisable lists (eg. enumerate/itemize, etc.)
\usepackage{verbatim} % adds environment for commenting out blocks of text & for better verbatim
% make it possible to include more than one captioned figure/table in a single float
% These packages are all incorporated in the memoir class to one degree or another...
% Gavin's handy macros
\newcommand{\chem}[1]{\ensuremath{\mathrm{#1}}}
\newcommand{\units}[1]{\ensuremath{\,\mathrm{#1}}}
\newcommand{\angstrom}{\text{\normalfont\AA}}
\newcommand{\degC}{\ensuremath{^\circ\units{C}}}
%set page header and footer
\usepackage{fancyhdr}
\usepackage{abstract}
\usepackage{blindtext}
\renewcommand{\abstractname}{}    % clear the title
\renewcommand{\absnamepos}{empty} % originally center
\usepackage[left=2cm,right=2cm,top=2cm,bottom=2cm]{geometry}

\pagestyle{fancy}
\lhead{\includegraphics[width=2cm]{SHRI_Logo}}
\chead{Application Note}
\rhead{\includegraphics*[width=4cm]{UoLeeds}}

%%% BibTex References
\usepackage[
backend=biber,
maxnames=1,
sorting=none,
style=science,
citestyle=numeric-comp
]{biblatex}
\addbibresource{AppNote.bib}
\DeclareSourcemap{
  \maps[datatype=bibtex]{
    \map{
      \step[fieldsource=url, match=\regexp{http(s?)://}, final]
      \step[fieldset=url, null]
      \step[fieldset=urldate, null]
    }
    \map{
      \step[fieldsource=doi, final]
      \step[fieldset=doi, null]
      \step[fieldset=isbn, null]
      \step[fieldset=issn, null]
    }
  }
}
\makeatletter
\def\@maketitle{%
	\begin{center}%
		\let \footnote \thanks
		{\LARGE \@title \par}%
		\vskip 0.5em%
		{\large
			\lineskip .5em%
			\begin{tabular}[t]{c}%
				\@author
			\end{tabular}\par}%
	\end{center}%
	\par}
\makeatother
\titlespacing\section{0pt}{10pt plus 4pt minus 2pt}{3pt plus 2pt minus 2pt}

